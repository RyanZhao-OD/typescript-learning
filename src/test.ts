// var options = {
//
// };
// options.color = "red";
// options.volume = 11;

interface Options { color: string; volume: number }

const options = {} as Options;
options.color = "red";
options.volume = 11;

console.log(options);
